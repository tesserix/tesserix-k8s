# Production overrides for network-policies
# Use with: helm upgrade --values values.yaml --values values-prod.yaml

# Enable stricter network policies for production
networkPolicies:
  enabled: true
  defaultDeny: true
