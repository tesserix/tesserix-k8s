apiVersion: v2
name: support-platform-namespace
description: Namespace and baseline Istio policies for the shared SLM support platform (Otto + slm-router + RAG + inference, serving mark8ly/fanzone/homechef tenants)
type: application
version: 0.1.1
appVersion: "v0.1.0"
