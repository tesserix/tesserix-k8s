apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fanzone-api.configMapName" . }}
  labels:
    {{- include "fanzone-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
