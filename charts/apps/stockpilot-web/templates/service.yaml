apiVersion: v1
kind: Service
metadata:
  name: {{ include "stockpilot-web.fullname" . }}
  labels:
    {{- include "stockpilot-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stockpilot-web.selectorLabels" . | nindent 4 }}
