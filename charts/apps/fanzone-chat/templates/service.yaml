apiVersion: v1
kind: Service
metadata:
  name: {{ include "fanzone-chat.fullname" . }}
  labels:
    {{- include "fanzone-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fanzone-chat.selectorLabels" . | nindent 4 }}
