apiVersion: v2
name: sm-version-cleanup
description: CronJob that enforces version retention on GCP Secret Manager — keeps the latest N active versions per secret and destroys older ones to control storage cost.
type: application
version: 0.1.0
appVersion: "1.0.0"
