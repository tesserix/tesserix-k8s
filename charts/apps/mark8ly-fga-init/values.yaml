namespace: mark8ly
name: mark8ly-fga-init

openfgaUrl: "http://mark8ly-openfga:8080"
storeName: "mark8ly-platform"

# curlimages/curl ships curl + jq pre-installed and runs as a non-root user.
# Avoids the apk-add-against-readonly-rootfs trap that the alpine base hit.
image:
  repository: curlimages/curl
  tag: "8.5.0"
