# UAT overlay for mark8ly-namespace.
namespace: mark8ly-uat
# Allowed inbound source namespaces for the mark8ly-uat namespace.
# Same shape as prod minus tesserix (the super-admin/company namespace
# only consumes prod APIs — UAT is intentionally isolated from it).
allowedSources:
  - istio-system
  - mark8ly-uat
