namespace: mark8ly
# Namespaces whose workloads are allowed to initiate traffic into mark8ly.
# Ingress comes through istio-system (gateway). Intra-namespace traffic is also allowed.
allowedSources:
  - istio-system
  - mark8ly
