# Zero-trust default-deny for the mark8ly namespace.
# Every workload must be explicitly allowed by a higher-priority ALLOW policy.
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: default-deny
  namespace: {{ .Values.namespace }}
spec:
  {}
