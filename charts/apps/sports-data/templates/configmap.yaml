apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sports-data.configMapName" . }}
  labels:
    {{- include "sports-data.labels" . | nindent 4 }}
data:
  VERSION: {{ .Chart.AppVersion | quote }}
