apiVersion: v1
kind: Service
metadata:
  name: {{ include "translation-service.fullname" . }}
  labels:
    {{- include "translation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "translation-service.selectorLabels" . | nindent 4 }}
