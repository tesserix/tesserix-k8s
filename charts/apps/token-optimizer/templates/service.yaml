apiVersion: v1
kind: Service
metadata:
  name: {{ include "token-optimizer.fullname" . }}
  labels:
    {{- include "token-optimizer.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "token-optimizer.selectorLabels" . | nindent 4 }}
  ports:
    - name: grpc
      port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
    - name: http
      port: {{ .Values.service.httpPort }}
      targetPort: http
      protocol: TCP

