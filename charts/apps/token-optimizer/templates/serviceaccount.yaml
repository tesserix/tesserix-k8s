{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "token-optimizer.serviceAccountName" . }}
  labels:
    {{- include "token-optimizer.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}

