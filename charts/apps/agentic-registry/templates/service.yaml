apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentic-registry.fullname" . }}
  labels: {{- include "agentic-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "agentic-registry.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
