apiVersion: v1
kind: Service
metadata:
  name: {{ include "hms-patient-portal.fullname" . }}
  labels:
    {{- include "hms-patient-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "hms-patient-portal.selectorLabels" . | nindent 4 }}
