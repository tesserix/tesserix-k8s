apiVersion: v1
kind: Service
metadata:
  name: {{ include "hms-staff-portal.fullname" . }}
  labels:
    {{- include "hms-staff-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "hms-staff-portal.selectorLabels" . | nindent 4 }}
