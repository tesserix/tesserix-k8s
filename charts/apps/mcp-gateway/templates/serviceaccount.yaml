apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mcp-gateway.name" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "mcp-gateway.labels" . | nindent 4 }}
automountServiceAccountToken: false
