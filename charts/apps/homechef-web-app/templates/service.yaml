apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.nameOverride | default (include "homechef-web-app.fullname" .) }}
  labels:
    {{- include "homechef-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "homechef-web-app.selectorLabels" . | nindent 4 }}
    homechef.tesserix.io/stack: deployment
