apiVersion: v1
kind: Service
metadata:
  name: {{ include "roamie-api.fullname" . }}
  labels:
    {{- include "roamie-api.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    {{- include "roamie-api.selectorLabels" . | nindent 4 }}
