{{- /*
Deliberately no ServiceAccount object here. helivanta-api already exists,
chart-managed by helivanta-postgres
(charts/apps/helivanta-postgres/templates/serviceaccount.yaml) — every
ExternalSecret across this namespace, including the two in this chart,
authenticates to OpenBao as it. `serviceAccount.create` is `false` in
values.yaml and this file intentionally renders nothing so a second owner
of the same object name never gets a chance to fight helivanta-postgres for
it in Argo's diff.
*/ -}}
