apiVersion: v1
kind: ResourceQuota
metadata:
  name: helivanta-compute
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/part-of: helivanta
    app.kubernetes.io/component: resource-governance
spec:
  hard:
{{ toYaml .Values.resourceQuota.hard | indent 4 }}
