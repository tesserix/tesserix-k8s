apiVersion: v2
name: helivanta-resource-quota
description: Namespace-wide ResourceQuota and LimitRange for Helivanta — bounds aggregate CPU/memory and gives every container defaults
type: application
version: 0.1.0
appVersion: "v0.1.0"
