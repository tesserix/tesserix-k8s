apiVersion: v1
kind: Service
metadata:
  name: {{ include "scrapper-mcp.fullname" . }}
  labels:
    {{- include "scrapper-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scrapper-mcp.selectorLabels" . | nindent 4 }}
