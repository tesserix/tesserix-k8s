apiVersion: v2
name: notification-hub
description: Notification Hub - Real-time in-app notifications service using NATS, WebSocket, and SSE
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: common
    version: "1.0.0"
    repository: "file://../common"
