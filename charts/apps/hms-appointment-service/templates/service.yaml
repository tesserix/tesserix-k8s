apiVersion: v1
kind: Service
metadata:
  name: {{ include "hms-appointment-service.fullname" . }}
  labels:
    {{- include "hms-appointment-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "hms-appointment-service.selectorLabels" . | nindent 4 }}
