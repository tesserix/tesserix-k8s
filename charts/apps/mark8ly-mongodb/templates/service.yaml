apiVersion: v1
kind: Service
metadata:
  name: {{ include "mark8ly-mongodb.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mark8ly-mongodb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: mongodb
      protocol: TCP
      name: mongodb
  selector:
    {{- include "mark8ly-mongodb.selectorLabels" . | nindent 4 }}
