apiVersion: v1
kind: Service
metadata:
  name: {{ include "hms-gateway.fullname" . }}
  labels:
    {{- include "hms-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "hms-gateway.selectorLabels" . | nindent 4 }}
