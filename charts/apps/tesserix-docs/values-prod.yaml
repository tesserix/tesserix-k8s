# Single replica in prod — low-traffic docs site. Disable PDB since
# minAvailable:1 with replicaCount:1 makes the pod permanently
# un-evictable (blocks every node drain).
replicaCount: 1

pdb:
  enabled: false
