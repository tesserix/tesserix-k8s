replicaCount: 2
image:
  repository: asia-south1-docker.pkg.dev/tesseracthub-480811/ghcr-remote/tesserix/tesserix-docs
  pullPolicy: IfNotPresent
  tag: "sha-930f6a7a633a" # CI rewrites this on every push to design-system/main
imagePullSecrets:
  - name: ghcr-secret
nameOverride: ""
fullnameOverride: ""
serviceAccount:
  create: true
  # Static web apps with no K8s API access — drop the auto-mounted SA token
  # to shrink blast radius if a container is compromised.
  automount: false
  annotations: {}
  name: ""
service:
  type: ClusterIP
  port: 80
  targetPort: 3000
ingress:
  enabled: true
  gateway: istio-ingress/tesseract-gateway
  hosts:
    - host: docs.tesserix.app
      paths:
        - path: /
          pathType: Prefix
resources:
  requests:
    memory: 256Mi
  limits:
    memory: 512Mi
podSecurityContext:
  fsGroup: 1001
  runAsNonRoot: true
  seccompProfile:
    type: RuntimeDefault
securityContext:
  runAsUser: 1001
  runAsNonRoot: true
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false
  capabilities:
    drop: ["ALL"]
volumes:
  tmp:
    enabled: true
    mountPath: /tmp
  cache:
    enabled: true
    mountPath: /app/apps/docs/.next/cache
pdb:
  enabled: false
# Page-view analytics via the self-hosted OpenPanel at analytics.tesserix.app.
# The client ID is a public write key; it lives in a Secret only because the
# project-init job is what mints it.
openpanel:
  enabled: true
  secretName: openpanel-client-ids
  clientIdKey: tesserix-docs-client-id
  apiUrl: https://analytics.tesserix.app/api
