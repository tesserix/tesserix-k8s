apiVersion: v1
kind: Service
metadata:
  name: {{ include "dwellm8-api.fullname" . }}
  labels:
    {{- include "dwellm8-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dwellm8-api.selectorLabels" . | nindent 4 }}
