apiVersion: v1
kind: Service
metadata:
  name: {{ include "fanzone-cleanup.fullname" . }}
  labels:
    {{- include "fanzone-cleanup.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default 8089 }}
      protocol: TCP
      name: http
  selector:
    {{- include "fanzone-cleanup.selectorLabels" . | nindent 4 }}
