{{- if .Values.externalSecrets.enabled }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "fanzone-media.fullname" . }}-secrets
  labels:
    {{- include "fanzone-media.labels" . | nindent 4 }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: {{ .Values.externalSecrets.secretStoreRef | default "gcp-secret-store" }}
    kind: ClusterSecretStore
  target:
    name: {{ include "fanzone-media.fullname" . }}-secrets
    creationPolicy: Owner
  data:
    - secretKey: GIPHY_API_KEY
      remoteRef:
        key: prod-fanzone-giphy-api-key
{{- end }}
