apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayPolicy
metadata:
  name: roamie-mcp-access
  namespace: agentgateway-system
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: roamie-roamie-travel-mcp
  traffic:
    jwtAuthentication:
      mode: Strict
      providers:
      - issuer: https://auth.tesserix.app
        audiences:
        - '387190457387450503'
        jwks:
          remote:
            backendRef:
              group: agentgateway.dev
              kind: AgentgatewayBackend
              name: ai-zitadel-jwks
              port: 443
            jwksPath: /oauth/v2/keys
            cacheDuration: 15m
    authorization:
      action: Allow
      policy:
        matchExpressions:
        - jwt.sub == {{ .Values.roamieSeed.managerSubject | toJson }} && "386377229942128837" in jwt["urn:zitadel:iam:org:project:387190457387450503:roles"]["roamie.manager"]
    timeouts:
      request: 55s
    rateLimit:
      local:
      - requests: 120
        burst: 16
        unit: Minutes
