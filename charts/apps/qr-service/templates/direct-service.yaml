{{- if .Values.knative.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "qr-service.fullname" . }}-direct
  labels:
    {{- include "qr-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    serving.knative.dev/service: {{ include "qr-service.fullname" . }}
{{- end }}
