apiVersion: v2
name: qr-service
description: QR Service - Go microservice for generating QR codes (URLs, WiFi, vCards, payments, etc.)
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: common
    version: "1.0.0"
    repository: "file://../common"
