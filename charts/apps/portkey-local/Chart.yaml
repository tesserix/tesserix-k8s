apiVersion: v2
name: portkey-local
description: Portkey AI Gateway (OSS) for the local sandbox — upstream ships no Helm chart, so this wraps the Deployment + Service
type: application
version: 0.1.0
appVersion: "latest"
