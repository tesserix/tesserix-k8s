# Portkey AI Gateway (OSS) — local sandbox. Stateless OpenAI-compatible proxy.
image: portkeyai/gateway:latest
port: 8787
replicas: 1
resources:
  requests:
    memory: 128Mi
  limits:
    memory: 512Mi
