{{- if .Values.registryOwnership.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: kora-ai-gateway-registry-ownership
  labels:
    {{- include "kora-ai-gateway.labels" . | nindent 4 }}
data:
  owner: agentic-registry
{{- end }}
