apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: {{ .Values.gateway.name }}
  labels:
    {{- include "devai-ai-gateway.labels" . | nindent 4 }}
spec:
  logging:
    format: json
    level: info
  resources:
    {{- toYaml .Values.gateway.resources | nindent 4 }}
  shutdown:
    min: 5
    max: 30
  istio:
    enabled: true
  deployment:
    spec:
      replicas: {{ .Values.gateway.replicas }}
      strategy:
        type: RollingUpdate
        rollingUpdate:
          maxUnavailable: 0
          maxSurge: 1
      template:
        spec:
          containers:
            - name: agentgateway
              env:
                - name: ADMIN_ADDR
                  value: {{ .Values.admin.address | quote }}
          securityContext:
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
          topologySpreadConstraints:
            - maxSkew: 1
              topologyKey: kubernetes.io/hostname
              whenUnsatisfiable: DoNotSchedule
              labelSelector:
                matchLabels:
                  gateway.networking.k8s.io/gateway-name: {{ .Values.gateway.name }}
            - maxSkew: 1
              topologyKey: topology.kubernetes.io/zone
              whenUnsatisfiable: ScheduleAnyway
              labelSelector:
                matchLabels:
                  gateway.networking.k8s.io/gateway-name: {{ .Values.gateway.name }}
  service:
    spec:
      type: ClusterIP
  serviceAccount:
    metadata:
      annotations:
        iam.gke.io/gcp-service-account: {{ .Values.gateway.serviceAccount.gcpServiceAccount | quote }}
  podDisruptionBudget:
    spec:
      minAvailable: 1
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ .Values.gateway.name }}
  labels:
    {{- include "devai-ai-gateway.labels" . | nindent 4 }}
spec:
  gatewayClassName: agentgateway
  infrastructure:
    parametersRef:
      group: agentgateway.dev
      kind: AgentgatewayParameters
      name: {{ .Values.gateway.name }}
  listeners:
    - name: http
      protocol: HTTP
      port: {{ .Values.gateway.port }}
      allowedRoutes:
        namespaces:
          from: Same
    - name: public
      protocol: HTTP
      port: {{ .Values.gateway.publicPort }}
      allowedRoutes:
        namespaces:
          from: Same
