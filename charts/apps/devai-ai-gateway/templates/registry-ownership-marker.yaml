{{- if .Values.registryOwnership.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: devai-ai-gateway-registry-ownership
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "devai-ai-gateway.labels" . | nindent 4 }}
data:
  owner: agentic-registry
{{- end }}
