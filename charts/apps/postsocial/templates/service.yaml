apiVersion: v1
kind: Service
metadata:
  name: {{ include "postsocial.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "postsocial.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postsocial.selectorLabels" . | nindent 4 }}
