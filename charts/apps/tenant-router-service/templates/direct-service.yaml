{{- if .Values.knative.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenant-router-service.fullname" . }}-direct
  labels:
    {{- include "tenant-router-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8089
      protocol: TCP
      targetPort: 8089
  selector:
    serving.knative.dev/service: {{ include "tenant-router-service.fullname" . }}
{{- end }}
