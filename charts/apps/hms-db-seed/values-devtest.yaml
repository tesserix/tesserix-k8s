# HMS Database Seed - DevTest Environment Values
# Development mode enabled with full dummy data

developmentEnabled: true
environment: devtest

database:
  host: "postgresql.database.svc.cluster.local"
  port: "5432"
  user: "postgres"
  sslmode: "disable"
  existingSecret: "postgresql-password"
  secretKey: "postgresql-password"

job:
  enabled: true
  restartPolicy: Never
  backoffLimit: 3
  ttlSecondsAfterFinished: 3600
