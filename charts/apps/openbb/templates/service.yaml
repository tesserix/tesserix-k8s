apiVersion: v1
kind: Service
metadata:
  name: {{ include "openbb.fullname" . }}
  labels:
    {{- include "openbb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "openbb.selectorLabels" . | nindent 4 }}
