apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fanzone-web.configMapName" . }}
  labels:
    {{- include "fanzone-web.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
