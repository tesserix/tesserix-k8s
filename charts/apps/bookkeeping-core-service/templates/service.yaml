apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookkeeping-core-service.fullname" . }}
  labels:
    {{- include "bookkeeping-core-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "bookkeeping-core-service.selectorLabels" . | nindent 4 }}
