apiVersion: v1
kind: Service
metadata:
  name: {{ include "fanzone-prediction-bot.fullname" . }}
  labels:
    {{- include "fanzone-prediction-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default 8088 }}
      protocol: TCP
      name: http
  selector:
    {{- include "fanzone-prediction-bot.selectorLabels" . | nindent 4 }}
