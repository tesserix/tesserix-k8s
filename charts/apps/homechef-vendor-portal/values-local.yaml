# LOCAL / kind overlay for homechef-vendor-portal. Use with:
#   helm upgrade --install homechef-vendor-portal charts/apps/homechef-vendor-portal \
#     -n homechef -f charts/apps/homechef-vendor-portal/values-local.yaml
#
# Build + load the image into kind first (no registry push):
#   docker build -t homechef-vendor-portal:local -f apps/vendor-portal/Dockerfile .
#   kind load docker-image homechef-vendor-portal:local
# Reach the SPA via:
#   kubectl -n homechef port-forward svc/homechef-vendor-portal 8080:80
#
# Note: VITE_* values are baked into the SPA at BUILD time (set as build args).
image:
  repository: homechef-vendor-portal
  tag: local
  pullPolicy: IfNotPresent

imagePullSecrets: []

replicaCount: 1

serviceAccount:
  create: true
  annotations: {}

env:
  VITE_API_URL: "http://localhost:8080/api/v1"
  VITE_WS_URL: "ws://localhost:8080/ws"
  VITE_APP_NAME: "HomeChef Vendor Portal (local)"
  VITE_APP_ENV: "local"

gcp:
  projectId: ""
  secretManager:
    secretPrefix: "local"

keda:
  enabled: false
autoscaling:
  enabled: false
vpa:
  enabled: false

podDisruptionBudget:
  enabled: false

ingress:
  enabled: false
