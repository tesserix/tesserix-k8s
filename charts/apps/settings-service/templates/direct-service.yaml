{{- if .Values.knative.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "settings-service.fullname" . }}-direct
  labels:
    {{- include "settings-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8085
  selector:
    serving.knative.dev/service: {{ include "settings-service.fullname" . }}
{{- end }}
