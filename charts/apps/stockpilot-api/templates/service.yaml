apiVersion: v1
kind: Service
metadata:
  name: {{ include "stockpilot-api.fullname" . }}
  labels:
    {{- include "stockpilot-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stockpilot-api.selectorLabels" . | nindent 4 }}
