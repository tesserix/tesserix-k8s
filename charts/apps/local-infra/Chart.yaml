apiVersion: v2
name: local-infra
description: Shared local-sandbox infrastructure — CNPG Postgres, Redis, NATS, MongoDB + per-product cleanup
type: application
version: 0.1.1
appVersion: "0.1.0"
# LOCAL/SANDBOX ONLY. Production uses the per-product *-postgres charts (CNPG),
# product Redis/NATS, etc. This chart provides ONE shared set of datastores for
# the local sandbox so every product auto-connects to the same endpoints.
