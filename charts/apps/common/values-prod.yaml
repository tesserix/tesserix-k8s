# Production overrides for common chart
# Use with: helm upgrade --values values.yaml --values values-prod.yaml

# Production-specific global configurations
global:
  environment: prod
  imageTag: latest
