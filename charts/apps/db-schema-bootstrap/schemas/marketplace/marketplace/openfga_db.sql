-- OpenFGA database
-- OpenFGA manages its own schema via built-in migrations on startup.
-- This file only ensures the database exists.
-- The bootstrap script creates the database from the filename.
SELECT 1;
