apiVersion: v2
name: global-valkey
description: Shared HA Valkey for all products — primary + replicas with Sentinel failover, fronted by HAProxy for non-Sentinel-aware clients
type: application
version: 0.1.0
appVersion: "9.1.1"
