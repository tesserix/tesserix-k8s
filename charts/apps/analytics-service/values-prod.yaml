knative:
  enabled: true
  minScale: 1
  scaleToZeroDelay: "120s"
  stableWindow: "300s"
externalSecrets:
  enabled: true
  secretStoreRef: gcp-secret-store
resources:
  limits:
    memory: 512Mi
  requests:
    memory: 128Mi

image:
  tag: "latest"
