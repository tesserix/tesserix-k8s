apiVersion: v2
name: local-root
description: Tesserix local sandbox app-of-apps — orders CNPG operator, shared local-infra datastores, and product apps via Argo sync-waves
type: application
version: 0.1.0
appVersion: "0.1.0"
# LOCAL/SANDBOX ONLY. A single root that brings the local Tesserix stack up in
# the right order: CNPG operator -> shared datastores (local-infra) -> products.
