{{- if .Values.keda.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    name: {{ .Values.name }}
  pollingInterval: {{ .Values.keda.pollingInterval }}
  cooldownPeriod: {{ .Values.keda.cooldownPeriod }}
  minReplicaCount: {{ .Values.keda.minReplicaCount }}
  maxReplicaCount: {{ .Values.keda.maxReplicaCount }}
  triggers:
    # Memory only, NOT cpu. The platform runs memory-only resources (no CPU
    # requests), so a KEDA `cpu` trigger is invalid here — the KEDA admission
    # webhook rejects a ScaledObject with a cpu trigger when the target
    # container has no cpu request, which made every auth-bff ArgoCD sync fail
    # and froze the deployment on a stale image.
    #
    # The istio request-rate (prometheus) trigger was removed (#906): the
    # monitoring namespace is scaled to zero, so prometheus-server has no
    # endpoints, the HPA could not compute the external metric, and the
    # resulting Degraded ScaledObject failed Kargo's health gate — silently
    # blocking every production promotion while CI stayed green. Restore it
    # here if monitoring comes back.
    - type: memory
      metricType: Utilization
      metadata:
        value: "80"
{{- end }}
