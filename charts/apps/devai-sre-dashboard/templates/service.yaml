apiVersion: v1
kind: Service
metadata:
  name: {{ include "devai-sre-dashboard.fullname" . }}
  labels:
    {{- include "devai-sre-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devai-sre-dashboard.selectorLabels" . | nindent 4 }}
