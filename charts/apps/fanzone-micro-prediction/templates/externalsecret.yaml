{{- if .Values.externalSecrets.enabled }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "fanzone-micro-prediction.fullname" . }}-secrets
  labels:
    {{- include "fanzone-micro-prediction.labels" . | nindent 4 }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: {{ .Values.externalSecrets.secretStoreRef | default "gcp-secret-store" }}
    kind: ClusterSecretStore
  target:
    name: {{ include "fanzone-micro-prediction.fullname" . }}-secrets
    creationPolicy: Owner
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: prod-fanzone-postgresql-url
    - secretKey: AUTH_DATABASE_URL
      remoteRef:
        key: prod-fanzone-auth-database-url
    - secretKey: JWT_SECRET
      remoteRef:
        key: prod-fanzone-jwt-secret
    - secretKey: NATS_URL
      remoteRef:
        key: prod-fanzone-nats-url
{{- end }}
