apiVersion: v2
name: helivanta-network-policies
description: Namespace-wide network policy for Helivanta — default-deny ingress and egress, with intra-namespace traffic, DNS, the ingress gateway, and external HTTPS/Zitadel egress explicitly allowed
type: application
version: 0.1.0
appVersion: "v0.1.0"
