apiVersion: v1
kind: Service
metadata:
  name: {{ include "stockpilot-worker.fullname" . }}
  labels:
    {{- include "stockpilot-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stockpilot-worker.selectorLabels" . | nindent 4 }}
