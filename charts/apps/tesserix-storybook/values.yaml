replicaCount: 2
image:
  repository: asia-south1-docker.pkg.dev/tesseracthub-480811/ghcr-remote/tesserix/tesserix-storybook
  pullPolicy: IfNotPresent
  tag: "sha-4b17e5d67d74" # CI rewrites this on every push to design-system/main
imagePullSecrets:
  - name: ghcr-secret
nameOverride: ""
fullnameOverride: ""
serviceAccount:
  create: true
  # Static web apps with no K8s API access — drop the auto-mounted SA token
  # to shrink blast radius if a container is compromised.
  automount: false
  annotations: {}
  name: ""
service:
  type: ClusterIP
  port: 80
  targetPort: 8080
ingress:
  enabled: true
  gateway: istio-ingress/tesseract-gateway
  hosts:
    - host: ui.tesserix.app
      paths:
        - path: /
          pathType: Prefix
resources:
  requests:
    memory: 64Mi
  limits:
    memory: 256Mi
podSecurityContext:
  fsGroup: 1001
  runAsNonRoot: true
  seccompProfile:
    type: RuntimeDefault
securityContext:
  runAsUser: 1001
  runAsNonRoot: true
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false
  capabilities:
    drop: ["ALL"]
volumes:
  tmp:
    enabled: true
    mountPath: /tmp
  nginxCache:
    enabled: true
    mountPath: /var/cache/nginx
pdb:
  enabled: false
