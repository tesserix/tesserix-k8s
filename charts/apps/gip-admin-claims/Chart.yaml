apiVersion: v2
name: gip-admin-claims
description: CronJob + on-demand Job that enforces the admin allow-list on the GIP Internal tenant — exactly the listed emails get the {admin:true} custom claim, every other user in the tenant loses it.
type: application
version: 0.1.1
appVersion: "v0.1.0"
