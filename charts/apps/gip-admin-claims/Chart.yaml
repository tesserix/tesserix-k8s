apiVersion: v2
name: gip-admin-claims
description: CronJob + on-demand Job enforcing the admin allow-list on the GIP Internal tenant — listed emails get {admin:true}, everyone else loses it.
type: application
version: 0.1.1
appVersion: "v0.1.0"
