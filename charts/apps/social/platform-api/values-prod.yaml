env: prod

resources:
  requests:
    cpu: 100m
    memory: 192Mi
  limits:
    memory: 384Mi

keda:
  enabled: true
  minReplicaCount: 1
  maxReplicaCount: 5

pdb:
  enabled: true
  maxUnavailable: 1
