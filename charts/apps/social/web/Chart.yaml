apiVersion: v2
name: social-web
description: Tesserix Social — user-facing Next.js web app (modern, clean, persisted theme toggle, AI-driven analytics reports). Routes social.tesserix.app → in-cluster auth-bff via the BFF rewrite.
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: common
    version: "1.0.0"
    repository: "file://../../common"
