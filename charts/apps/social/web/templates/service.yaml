apiVersion: v1
kind: Service
metadata:
  name: {{ include "social-web.fullname" . }}
  labels:
    {{- include "social-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "social-web.selectorLabels" . | nindent 4 }}
