# Production overrides for homechef-api
# Use with: helm upgrade --values values.yaml --values values-prod.yaml

# Knative Serving - scale-to-zero for cost optimization
knative:
  enabled: true
  minScale: 1
  maxScale: 10
  scaleDownDelay: "60s"

replicaCount: 2

# Production resource allocation
resources:
  limits:
    memory: 2Gi
  requests:
    memory: 512Mi

# Production environment
env:
  GIN_MODE: "release"
  LOG_LEVEL: "warn"

# Disable HPA and KEDA — Knative handles autoscaling
autoscaling:
  enabled: false
# Production GCP configuration
gcp:
  projectId: "tesseracthub-480811"
  workloadIdentity:
    enabled: true
    gcpServiceAccount: "app-secrets-homechef-prod@tesseracthub-480811.iam.gserviceaccount.com"
  secretManager:
    enabled: true
    secretPrefix: "prod"

keda:
  enabled: false

# Production database
database:
  host: "postgresql.postgresql-homechef.svc.cluster.local"
  port: "5432"
  user: "homechef"
  name: "homechef"
  sslmode: "require"

# Production Redis
redis:
  host: "redis.redis-homechef.svc.cluster.local"
  port: "6379"
