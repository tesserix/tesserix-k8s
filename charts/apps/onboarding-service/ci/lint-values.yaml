# chart-testing renders with default values, and the console sign-in settings
# are per-environment values the chart deliberately guards. These throwaway ones
# let lint exercise the templates instead of stopping at the guard.
console:
  googleClientId: ci-lint-placeholder.apps.googleusercontent.com
  adminEmails:
    - ci-lint@example.invalid
