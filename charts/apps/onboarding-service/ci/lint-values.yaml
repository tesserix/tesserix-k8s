# chart-testing renders with default values, and the real consoleClientId is a
# per-environment value the chart deliberately `required`s. This supplies a
# throwaway one so lint exercises the template instead of stopping at the guard.
zitadel:
  consoleClientId: ci-lint-placeholder
