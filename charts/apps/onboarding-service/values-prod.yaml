# The platform org id is deliberately absent: the API resolves it from
# zitadel.platformOrgName at boot, so there is no hand-copied id to go stale.
console:
  # Google Cloud -> Google Auth Platform -> Clients -> onboard.tesserix.app.
  # Authorised origin https://onboard.tesserix.app, redirect
  # https://onboard.tesserix.app/api/auth/callback.
  googleClientId: 849928263410-850rm48bo386qc7u6lav4u69u6mrssc8.apps.googleusercontent.com
  # Add or remove a console user here; the API re-checks this list on every
  # request, so a removal takes effect on the next sync rather than in 8 hours.
  adminEmails:
    - samyak.rout@gmail.com
    - unidevidp@gmail.com
    - mahesh.sangawar@gmail.com

api:
  replicaCount: 2
web:
  replicaCount: 2
