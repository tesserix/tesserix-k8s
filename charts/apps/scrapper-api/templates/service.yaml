apiVersion: v1
kind: Service
metadata:
  name: {{ include "scrapper-api.fullname" . }}
  labels:
    {{- include "scrapper-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scrapper-api.selectorLabels" . | nindent 4 }}
