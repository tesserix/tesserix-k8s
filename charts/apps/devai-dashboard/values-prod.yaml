# Production overrides for devai-dashboard

replicaCount: 1

resources:
  requests:
    memory: 128Mi
  limits:
    memory: 512Mi

env:
  NODE_ENV: "production"
  NEXT_PUBLIC_API_URL: "https://devai.tesserix.app"

gcp:
  projectId: "tesseracthub-480811"
  workloadIdentity:
    enabled: true
    gcpServiceAccount: "app-secrets-devai-prod@tesseracthub-480811.iam.gserviceaccount.com"
