{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "homechef-admin-portal.fullname" . }}
  labels:
    {{- include "homechef-admin-portal.labels" . | nindent 4 }}
spec:
  maxUnavailable: {{ .Values.podDisruptionBudget.maxUnavailable | default 1 }}
  selector:
    matchLabels:
      {{- include "homechef-admin-portal.selectorLabels" . | nindent 6 }}
{{- end }}
