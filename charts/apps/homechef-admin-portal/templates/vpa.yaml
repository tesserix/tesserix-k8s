{{- if and .Values.vpa.enabled (not .Values.knative.enabled) }}
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: {{ include "homechef-admin-portal.fullname" . }}
  labels:
    {{- include "homechef-admin-portal.labels" . | nindent 4 }}
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "homechef-admin-portal.fullname" . }}
  updatePolicy:
    updateMode: {{ .Values.vpa.updateMode | quote }}
  resourcePolicy:
    containerPolicies:
      - containerName: {{ .Chart.Name }}
        minAllowed:
          memory: {{ .Values.vpa.minAllowed.memory | quote }}
        maxAllowed:
          memory: {{ .Values.vpa.maxAllowed.memory | quote }}
        controlledResources: ["cpu", "memory"]
      {{- if .Values.vpa.excludeIstioProxy }}
      - containerName: istio-proxy
        mode: "Off"
      {{- end }}
{{- end }}
