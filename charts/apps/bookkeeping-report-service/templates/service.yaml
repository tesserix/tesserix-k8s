apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookkeeping-report-service.fullname" . }}
  labels:
    {{- include "bookkeeping-report-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "bookkeeping-report-service.selectorLabels" . | nindent 4 }}
