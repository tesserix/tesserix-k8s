apiVersion: v1
kind: Service
metadata:
  name: {{ include "huggingface-mt-service.fullname" . }}
  labels:
    {{- include "huggingface-mt-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "huggingface-mt-service.selectorLabels" . | nindent 4 }}
