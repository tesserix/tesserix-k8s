# Production overrides for huggingface-mt-service

image:
  repository: ghcr.io/tesserix/marketplace-services/huggingface-mt-service
  tag: "1.0.1"
# Use with: helm upgrade --values values.yaml --values values-prod.yaml

replicaCount: 2

# Production resource allocation (ML model requires more resources)
resources:
  limits:
    memory: 8Gi
  requests:
    memory: 4Gi

# KEDA autoscaling - enabled for production
keda:
  enabled: true
  minReplicaCount: 2
  maxReplicaCount: 6
  pollingInterval: 30
  cooldownPeriod: 600  # Longer cooldown for ML models
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "70"
    - type: memory
      metricType: Utilization
      metadata:
        value: "80"

# Pod Disruption Budget - enabled for production HA
pdb:
  enabled: true
  minAvailable: 1

# Production GCP configuration
gcp:
  secretManager:
    secretPrefix: "prod"
