apiVersion: v2
name: dwellm8-network-policies
description: Namespace-wide network policy for Dwellm8 — default-deny ingress, with observability and the in-namespace Temporal and NATS explicitly allowed
type: application
version: 0.1.0
appVersion: "v0.1.0"
