apiVersion: v2
name: infra-postgres
description: Shared HA PostgreSQL (CloudNativePG) for platform components — Temporal first, others by adding a database rather than a cluster
type: application
version: 0.1.4
appVersion: "16"
