apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fanzone-ws.configMapName" . }}
  labels:
    {{- include "fanzone-ws.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
