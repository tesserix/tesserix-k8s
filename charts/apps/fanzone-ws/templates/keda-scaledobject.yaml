{{- if .Values.keda.enabled }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "fanzone-ws.fullname" . }}
  labels:
    {{- include "fanzone-ws.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    name: {{ include "fanzone-ws.fullname" . }}
  pollingInterval: {{ .Values.keda.pollingInterval | default 15 }}
  cooldownPeriod: {{ .Values.keda.cooldownPeriod | default 600 }}
  minReplicaCount: {{ .Values.keda.minReplicaCount | default 1 }}
  maxReplicaCount: {{ .Values.keda.maxReplicaCount | default 100 }}
  triggers:
    {{- range .Values.keda.triggers }}
    - type: {{ .type }}
      metricType: {{ .metricType }}
      metadata:
        {{- toYaml .metadata | nindent 8 }}
    {{- end }}
{{- end }}
