{{- if .Values.externalSecrets.enabled }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "fanzone-book-cricket.fullname" . }}-secrets
  labels:
    {{- include "fanzone-book-cricket.labels" . | nindent 4 }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: {{ .Values.externalSecrets.secretStoreRef | default "gcp-secret-store" }}
    kind: ClusterSecretStore
  target:
    name: {{ include "fanzone-book-cricket.fullname" . }}-secrets
    creationPolicy: Owner
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: prod-fanzone-postgresql-url
    - secretKey: POINTS_DATABASE_URL
      remoteRef:
        key: prod-fanzone-auth-postgresql-url
    - secretKey: JWT_SECRET
      remoteRef:
        key: prod-fanzone-jwt-secret
    - secretKey: NATS_URL
      remoteRef:
        key: prod-fanzone-nats-url
    - secretKey: CRICKETDATA_API_KEY
      remoteRef:
        key: prod-fanzone-cricketdata-api-key
    - secretKey: MONGO_URL
      remoteRef:
        key: prod-fanzone-mongodb-url
{{- end }}
