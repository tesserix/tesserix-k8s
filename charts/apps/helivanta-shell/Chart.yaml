apiVersion: v2
name: helivanta-shell
description: Helivanta's shell frontend — the Next standalone app that owns "/" behind helivanta.app (#824 slice 1b)
type: application
version: 0.1.0
appVersion: "0.1.0"
