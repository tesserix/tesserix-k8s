apiVersion: v1
kind: Service
metadata:
  name: {{ include "scrapper-web.fullname" . }}
  labels:
    {{- include "scrapper-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scrapper-web.selectorLabels" . | nindent 4 }}
