replicaCount: 1

image:
  repository: ghcr.io/tesseract-nexus/global-services/auth-bff
  pullPolicy: Always
  tag: "latest"

imagePullSecrets:
  - name: ghcr-secret

service:
  type: ClusterIP
  port: 8090
  targetPort: 8090

serviceAccount:
  create: true
  name: devai-auth-bff

# General config
env:
  PORT: "8090"
  HOST: "0.0.0.0"
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  TRUST_PROXY: "true"
  SESSION_MAX_AGE: "86400"
  SESSION_COOKIE_NAME: "devai_session"
  STOREFRONT_SESSION_COOKIE_NAME: "devai_session"
  HOME_SESSION_COOKIE_NAME: "devai_session"
  SESSION_COOKIE_DOMAIN: ""
  SESSION_COOKIE_SECURE: "true"
  SESSION_COOKIE_HTTPONLY: "true"
  SESSION_COOKIE_SAMESITE: "lax"
  BASE_DOMAIN: "tesserix.app"
  ALLOWED_ORIGINS: "https://devai.tesserix.app,https://sre.tesserix.app,https://*.tesserix.app"
  API_GATEWAY_URL: "http://devai-api.devai.svc.cluster.local:8080"

# DUAL-REALM KEYCLOAK CONFIGURATION
# - Customer realm (devai): ALM dashboard authentication
# - Internal realm (devai-sre): SRE dashboard authentication
# Both realms live on the identity-internal Keycloak instance
keycloak:
  customer:
    url: "https://identity-devai.tesserix.app"
    internalUrl: "http://keycloak.identity-internal.svc.cluster.local:8080"
    realm: "devai"
    clientId: "devai-bff"
  internal:
    url: "https://identity-sre.tesserix.app"
    internalUrl: "http://keycloak.identity-internal.svc.cluster.local:8080"
    realm: "devai-sre"
    clientId: "devai-sre-bff"

# Redis for sessions
redis:
  host: "redis.redis-devai.svc.cluster.local"
  port: "6379"

# External Secrets — managed via ClusterSecretStore "gcp-secret-store"
# Sources every key from GCP Secret Manager (project tesseracthub-480811).
# Was previously disabled; that left the in-cluster ExternalSecret stale
# and caused the pod to fail with `couldn't find key CSRF_SECRET in
# Secret devai/devai-auth-bff-secrets` when the deployment template
# added the CSRF_SECRET env var without anyone updating the manually-
# managed K8s Secret.
externalSecrets:
  enabled: true
  secretStoreRef: "gcp-secret-store"

resources:
  requests:
    memory: 128Mi
  limits:
    memory: 256Mi

podAnnotations:
