apiVersion: v1
kind: Service
metadata:
  name: {{ include "yes-hospital.fullname" . }}
  labels:
    {{- include "yes-hospital.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yes-hospital.selectorLabels" . | nindent 4 }}
