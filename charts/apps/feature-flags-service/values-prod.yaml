knative:
  enabled: false

# Production overrides for feature-flags-service

image:
  repository: ghcr.io/tesserix/mark8ly-feature-flags-service

# Use with: helm upgrade --values values.yaml --values values-prod.yaml

replicaCount: 1

# Production resource allocation
resources:
  limits:
    memory: 512Mi
  requests:
    memory: 128Mi

# Production environment
env:
  GIN_MODE: "release"
  ENVIRONMENT: "production"
  GROWTHBOOK_API_HOST: "https://growthbook.mark8ly.com"

# KEDA autoscaling - enabled for production
# Production GCP configuration
gcp:
  secretManager:
    secretPrefix: "prod"

keda:
  enabled: true
  minReplicaCount: 2
  maxReplicaCount: 10
  pollingInterval: 30
  cooldownPeriod: 300
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "70"
    - type: memory
      metricType: Utilization
      metadata:
        value: "75"

# Pod Disruption Budget - enabled for production HA
pdb:
  enabled: true
  minAvailable: 1
