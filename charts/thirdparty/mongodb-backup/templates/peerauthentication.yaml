{{- if .Values.istio.disableMtls }}
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: {{ include "mongodb-backup.fullname" . }}-disable-mtls
  labels:
    {{- include "mongodb-backup.labels" . | nindent 4 }}
spec:
  mtls:
    mode: DISABLE
{{- end }}
