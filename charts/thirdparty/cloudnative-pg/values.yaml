cloudnative-pg:
  # Operator runs cluster-wide in its own namespace (cnpg-system).
  # Each PG cluster it manages lives in its tenant namespace (e.g. mark8ly).
  replicaCount: 1
  monitoring:
    podMonitorEnabled: false
    grafanaDashboard:
      create: false
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi
