apiVersion: v1
kind: Service
metadata:
  name: mongodb-blog
  labels:
    app: mongodb-blog
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 27017
      protocol: TCP
      name: mongodb
  selector:
    app: mongodb-blog
