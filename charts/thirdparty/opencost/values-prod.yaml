# Production overrides for OpenCost

# OAuth2 Proxy - restrict access to Tesserix team
oauth2Proxy:
  # Only allow specific Google accounts
  allowedEmails:
    - samyak.rout@gmail.com
    - unidevidp@gmail.com
    - mahesh.sangawar@gmail.com

# External Secrets - production GCP Secret Manager keys
externalSecret:
  secrets:
    googleClientId: prod-google-client-id
    googleClientSecret: prod-google-client-secret
    cookieSecret: prod-opencost-cookie-secret
