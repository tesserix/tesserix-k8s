apiVersion: v2
name: zitadel
description: Zitadel identity platform — multi-tenant OIDC/SAML with per-organization IdP federation
type: application
version: 0.2.0
appVersion: "v4.15.3"
dependencies:
  - name: zitadel
    version: "10.0.4"
    repository: "https://charts.zitadel.com"
