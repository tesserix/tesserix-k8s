{{/*
Certificates for Istio Gateway TLS termination
These certificates must be in the istio-ingress namespace for the Gateway to use them
*/}}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: admin-tls
  namespace: istio-ingress
  labels:
    {{- include "istio-config.labels" . | nindent 4 }}
spec:
  secretName: admin-tls
  issuerRef:
    name: {{ .Values.tls.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - "{{ .Values.subdomains.admin }}.{{ .Values.domain }}"
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: storefront-tls
  namespace: istio-ingress
  labels:
    {{- include "istio-config.labels" . | nindent 4 }}
spec:
  secretName: storefront-tls
  issuerRef:
    name: {{ .Values.tls.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - "{{ .Values.subdomains.store }}.{{ .Values.domain }}"
---
{{- if .Values.subdomains.onboarding }}
# Certificate for tenant onboarding subdomain
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tenant-onboarding-tls
  namespace: istio-ingress
  labels:
    {{- include "istio-config.labels" . | nindent 4 }}
spec:
  secretName: tenant-onboarding-tls
  issuerRef:
    name: {{ .Values.tls.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - "{{ .Values.subdomains.onboarding }}.{{ .Values.domain }}"
{{- else }}
# Certificate for apex domain (tenant onboarding uses apex in prod)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: apex-domain-tls
  namespace: istio-ingress
  labels:
    {{- include "istio-config.labels" . | nindent 4 }}
spec:
  secretName: apex-domain-tls
  issuerRef:
    name: {{ .Values.tls.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - "{{ .Values.domain }}"
{{- end }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-tls
  namespace: istio-ingress
  labels:
    {{- include "istio-config.labels" . | nindent 4 }}
spec:
  secretName: api-tls
  issuerRef:
    name: {{ .Values.tls.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - "{{ .Values.subdomains.api }}.{{ .Values.domain }}"
# fanzone-web-tls Certificate removed 2026-07-23 — FanZone decommissioned.
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: growthbook-tls
  namespace: istio-ingress
  labels:
    {{- include "istio-config.labels" . | nindent 4 }}
spec:
  secretName: growthbook-tls
  issuerRef:
    name: {{ .Values.tls.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - "{{ .Values.subdomains.growthbook }}.{{ .Values.domain }}"
---
# Web Analytics certificate (OpenPanel)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: analytics-tls
  namespace: istio-ingress
  labels:
    {{- include "istio-config.labels" . | nindent 4 }}
spec:
  secretName: analytics-tls
  issuerRef:
    name: {{ .Values.tls.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    {{- if .Values.analyticsHost }}
    - "{{ .Values.analyticsHost }}"
    {{- else }}
    - "{{ .Values.subdomains.analytics }}.{{ .Values.domain }}"
    {{- end }}
---
# API Documentation certificate (Swagger UI)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-docs-tls
  namespace: istio-ingress
  labels:
    {{- include "istio-config.labels" . | nindent 4 }}
spec:
  secretName: api-docs-tls
  issuerRef:
    name: {{ .Values.tls.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - "{{ .Values.subdomains.apiDocs }}.{{ .Values.domain }}"
---
# Wildcard certificate for multi-tenant storefronts
# Enables <slug>.tesserix.app subdomain routing
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: storefront-wildcard-tls
  namespace: istio-ingress
  labels:
    {{- include "istio-config.labels" . | nindent 4 }}
spec:
  secretName: storefront-wildcard-tls
  issuerRef:
    name: {{ .Values.tls.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - "*.{{ .Values.domain }}"
{{- if .Values.companyHost }}
---
# Company website certificate (tesserix.app)
# Cloud Run only — routed via ILB + Istio ServiceEntry
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: company-tls
  namespace: istio-ingress
  labels:
    {{- include "istio-config.labels" . | nindent 4 }}
spec:
  secretName: company-tls
  issuerRef:
    name: {{ .Values.tls.clusterIssuer }}
    kind: ClusterIssuer
  dnsNames:
    - "{{ .Values.companyHost }}"
{{- end }}
