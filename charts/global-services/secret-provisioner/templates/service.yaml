apiVersion: v1
kind: Service
metadata:
  name: {{ include "secret-provisioner.fullname" . }}
  labels:
    {{- include "secret-provisioner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secret-provisioner.selectorLabels" . | nindent 4 }}
