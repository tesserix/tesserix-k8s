# Grafana GitHub OAuth credentials — synced from GCP Secret Manager so users
# log into grafana.tesserix.app with their GitHub (tesserix org) identity.
#
# The grafana chart references this secret via `envFromSecrets:
# grafana-github-oauth` (argocd/prod/infrastructure/grafana.yaml); the keys
# below become GF_AUTH_GITHUB_CLIENT_ID / GF_AUTH_GITHUB_CLIENT_SECRET env
# vars, which Grafana maps onto auth.github.client_id / client_secret.
#
# Credentials are the "Tesserix Platform SSO" GitHub App — the SAME real App
# ArgoCD and Kargo already use for GitHub login (prod-argocd-github-client-*).
# Reusing it means one SSO identity across the platform consoles. That App MUST
# list https://grafana.tesserix.app/login/github as a callback URL (GitHub Apps
# accept multiple, alongside the existing ArgoCD/Kargo callbacks) or GitHub
# returns redirect_uri_mismatch.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: grafana-github-oauth
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-store
    kind: ClusterSecretStore
  target:
    name: grafana-github-oauth
    creationPolicy: Owner
  data:
    - secretKey: GF_AUTH_GITHUB_CLIENT_ID
      remoteRef:
        key: prod-argocd-github-client-id
    - secretKey: GF_AUTH_GITHUB_CLIENT_SECRET
      remoteRef:
        key: prod-argocd-github-client-secret
