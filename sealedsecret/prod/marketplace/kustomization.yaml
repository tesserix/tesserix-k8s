apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# marketplace namespace Sealed Secrets
resources:
  # - ghcr-secret.yaml  # Removed — corrupted encrypted data, replaced by ExternalSecret ghcr-secret
# Uncomment as secrets are generated:
# - postgresql-password.yaml
# - redis-password.yaml
# - jwt-secret.yaml
