apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Pilot Sealed Secrets
# These secrets are generated using kubeseal with pilot/sealed-secrets.crt
resources:
  - infrastructure
  - global
  - marketplace
  # - hms       # Enable when HMS namespace is active
  # - fanzone   # Enable when FanZone namespace is active
  # - homechef  # Enable when HomeChef namespace is active
