apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - virtualservice.yaml
  - authorization-policy.yaml
  # Cross-namespace allowance so the registry (agentregistry-system) can reach
  # the shared devai-postgres cluster it reuses for its `registry` schema.
  - networkpolicy-devai-db.yaml
