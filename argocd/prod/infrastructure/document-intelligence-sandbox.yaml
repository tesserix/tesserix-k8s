apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: document-intelligence-sandbox
  namespace: argocd
  finalizers: [resources-finalizer.argocd.argoproj.io]
  labels: {app.kubernetes.io/part-of: document-intelligence, environment: sandbox}
spec:
  project: infrastructure
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    targetRevision: HEAD
    path: charts/apps/document-intelligence
    helm:
      releaseName: document-intelligence-sandbox
      valueFiles: [values-sandbox.yaml]
  destination: {server: https://kubernetes.default.svc, namespace: document-intelligence}
  syncPolicy:
    automated: {prune: true, selfHeal: true}
    syncOptions: [CreateNamespace=true, ServerSideApply=true]
    retry: {limit: 5, backoff: {duration: 5s, factor: 2, maxDuration: 3m}}
