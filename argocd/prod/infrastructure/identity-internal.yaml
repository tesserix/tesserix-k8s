apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: identity-internal
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/part-of: tesserix
    app.kubernetes.io/component: identity
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: identity
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    path: charts/thirdparty/identity-internal
    targetRevision: HEAD
    helm:
      releaseName: identity-internal
      valueFiles:
        - values.yaml
      values: |
        # Production specific overrides
        keycloak:
          replicaCount: 2
          hostname: ""

          # Production resources — Keycloak 26 needs ~1.5GB minimum
          # JVM heap (-Xmx) must be < 60% of memory limit for non-heap headroom
          javaOpts: "-Xms384m -Xmx768m -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:MaxMetaspaceSize=256m"
          resources:
            requests:
              memory: 768Mi
            limits:
              memory: 1536Mi

          # Kong ingress for external access
          ingress:
            enabled: true
            className: "kong"
            clusterIssuer: "letsencrypt-prod"
            hosts:
              - host: internal-identity.fe3dr.com
                paths:
                  - path: /
                    pathType: Prefix
            tls:
              - secretName: internal-idp-tls
                hosts:
                  - internal-identity.fe3dr.com

          # Disable autoscaling for internal (always fixed)
          autoscaling:
            enabled: false

          # External Secrets — production GCP secret names.
          # Mirrors external-secrets/prod/identity-internal/externalsecret.yaml so
          # the helm-templated and standalone ExternalSecrets converge on the same
          # data and never overwrite each other with stale dev refs.
          externalSecrets:
            enabled: true
            secrets:
              adminBffClientSecret: "prod-keycloak-admin-bff-client-secret"
              tokenExchangeClientSecret: "prod-keycloak-token-exchange-client-secret"
              adminClientSecret: "prod-keycloak-internal-admin-client-secret"
              dashboardClientSecret: "prod-keycloak-internal-dashboard-client-secret"
              # Shared with charts/apps/homechef-auth-bff/templates/externalsecret.yaml
              homechefAdminBffClientSecret: "prod-homechef-internal-keycloak-client-secret"
              # Shared with charts/apps/secret-service/templates/externalsecret.yaml
              secretServiceClientSecret: "prod-secret-service-oidc-client-secret"

        # PostgreSQL - smaller for devtest/internal
        keycloak-db:
          primary:
            persistence:
              size: 10Gi
            resources:
              requests:
                memory: 256Mi
              limits:
                memory: 1Gi
          readReplicas:
            replicaCount: 1
            persistence:
              size: 10Gi
          metrics:
            enabled: false  # Disable until image version is fixed

        # Redis - standalone for devtest/internal
        keycloak-redis:
          architecture: standalone  # Use standalone instead of replication
          sentinel:
            enabled: false  # Must disable sentinel for standalone mode
          master:
            persistence:
              size: 1Gi
            resources:
              requests:
                memory: 128Mi
              limits:
                memory: 512Mi
          metrics:
            enabled: false  # Disable until image version is fixed

        # Admin SSO Configuration
        adminSSO:
          enabled: true
          googleCredentialsSecret: "keycloak-google-sso"
          superAdmins:
            - "unidevidp@gmail.com"
            - "mahesh.sangawar@gmail.com"
            - "samyak.rout@gmail.com"
          disablePasswordLogin: false  # Keep password as break-glass

  destination:
    server: https://kubernetes.default.svc
    namespace: identity-internal
  ignoreDifferences:
    - group: apps
      kind: StatefulSet
      jsonPointers:
        - /status
    # Kubernetes 1.31+ added .status.terminatingReplicas to Deployment, which
    # ArgoCD's structured-merge OpenAPI schema doesn't yet know about. Without
    # this, every diff fails with "field not declared in schema" and the app
    # never reaches Synced — blocking the realm-import and bootstrap post-hooks.
    - group: apps
      kind: Deployment
      jsonPointers:
        - /status
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
