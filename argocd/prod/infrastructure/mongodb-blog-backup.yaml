apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mongodb-blog-backup
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/part-of: tesseract-platform
    app.kubernetes.io/component: backup
    target-namespace: db-backup-and-restore
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: data
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    path: charts/thirdparty/mongodb-backup
    targetRevision: HEAD
    helm:
      releaseName: mongodb-blog-backup
      values: |
        backup:
          enabled: true
          # Twice daily: 00:00 and 12:00 UTC
          schedule: "0 0,12 * * *"

          # Database connection
          database:
            host: mongodb-blog.mongodb-blog.svc.cluster.local
            port: 27017
            authDatabase: admin
            user: admin
            authEnabled: true
            passwordSecretName: mongodb-blog-password
            passwordSecretKey: mongodb-password

          # Backup settings - backup ALL databases
          settings:
            allDatabases: true
            excludeDatabases:
              - admin
              - local
              - config

          # GCS Configuration
          gcs:
            bucket: tesseract-database-backups
            prefix: prod/mongodb-blog

        # Network Policy - disabled
        networkPolicy:
          enabled: false

        # Istio - disable mTLS since backup pods don't have sidecars
        istio:
          disableMtls: true

        # Reuse postgres-backup SA (already has Workload Identity binding)
        serviceAccount:
          create: false
          name: postgres-backup

        # Resources for backup job
        resources:
          requests:
            memory: 256Mi
          limits:
            memory: 1Gi

  destination:
    server: https://kubernetes.default.svc
    namespace: db-backup-and-restore
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
