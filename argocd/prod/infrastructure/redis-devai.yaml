apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redis-devai
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/part-of: devai
    app.kubernetes.io/component: cache
    target-namespace: devai
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  project: data
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    path: charts/thirdparty/redis
    targetRevision: HEAD
    helm:
      releaseName: redis
      values: |
        # Redis for DevAI pipeline state
        auth:
          enabled: false

        resources:
          requests:
            memory: 128Mi
          limits:
            memory: 512Mi

        persistence:
          enabled: true
          storageClass: standard-rwo
          size: 2Gi

        service:
          type: ClusterIP
          port: 6379

  destination:
    server: https://kubernetes.default.svc
    namespace: redis-devai
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - Replace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
