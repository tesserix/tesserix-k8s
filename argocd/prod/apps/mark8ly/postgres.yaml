apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mark8ly-postgres
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-30"
  labels:
    product: mark8ly
    environment: prod
    component: database
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    targetRevision: HEAD
    path: charts/apps/mark8ly-postgres
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: mark8ly
  syncPolicy:
    automated:
      # prune intentionally OMITTED (it defaults to false) — never prune a database via GitOps accidentally.
      # Declaring `prune: false` explicitly makes the app permanently
      # OutOfSync: false is the zero value, so the API server drops the
      # key and the app-of-apps diffs git (present) vs live (absent).
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
