apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: scrapper-istio
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: social-media-scrapper
    app.kubernetes.io/component: routing
    environment: prod
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: scrapper
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    targetRevision: HEAD
    path: manifests/scrapper-istio
  destination:
    server: https://kubernetes.default.svc
    namespace: scrapper
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
