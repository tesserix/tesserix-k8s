apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: agentic-istio
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: tesserix
    app.kubernetes.io/component: routing
    environment: prod
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    targetRevision: HEAD
    path: manifests/agentic-istio
  destination:
    server: https://kubernetes.default.svc
    # The kustomization spans kagent-system + agentregistry-system —
    # ArgoCD applies based on the namespace embedded in each resource.
    namespace: kagent-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
