apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: stockpilot-istio
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: stock-analysis
    app.kubernetes.io/component: routing
    environment: prod
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: stockpilot
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    targetRevision: HEAD
    path: manifests/stockpilot-istio
  destination:
    server: https://kubernetes.default.svc
    namespace: stockpilot
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
