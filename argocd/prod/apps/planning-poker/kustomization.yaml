apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Database, then the room backplane — the API dials both on startup.
  - planning-poker-postgres.yaml
  - planning-poker-nats.yaml
  - planning-poker.yaml
