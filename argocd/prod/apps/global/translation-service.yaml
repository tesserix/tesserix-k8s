apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: translation-service
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: tesserix-prod:translation-service-prod
  labels:
    app.kubernetes.io/part-of: tesserix
    app.kubernetes.io/component: backend
    environment: prod
spec:
  project: platform
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    targetRevision: HEAD
    path: charts/apps/translation-service
    helm:
      valueFiles:
      - values.yaml
      - values-prod.yaml
      - ../global-config-prod.yaml
      parameters:
      - name: image.tag
        value: "1.3.5"
      - name: database.host
        value: postgresql.postgresql-marketplace.svc.cluster.local
      - name: env.REDIS_HOST
        value: redis.redis-marketplace.svc.cluster.local
      - name: gcp.workloadIdentity.gcpServiceAccount
        value: app-secrets-marketplace@tesseracthub-480811.iam.gserviceaccount.com
  destination:
    server: https://kubernetes.default.svc
    namespace: marketplace
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
