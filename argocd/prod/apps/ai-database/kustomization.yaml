apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# AI data stores. Anything listed here must also exist as a file — and any
# file added here must be listed, or Kustomize silently never builds it.
resources:
  - ai-database-namespace.yaml
  - qdrant.yaml
