apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: guardix-school-admin
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: guardix
    app.kubernetes.io/component: school-admin
    environment: prod
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: guardix
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    targetRevision: HEAD
    path: charts/apps/guardix-school-admin
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: guardix
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
