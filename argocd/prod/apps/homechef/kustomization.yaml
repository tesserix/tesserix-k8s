apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - homechef-api.yaml
  # HA Temporal experiment removed 2026-07-23 — cluster is at its per-node pod
  # ceiling (110/110) so the 8-pod HA control plane never scheduled and thrashed,
  # adding pressure that starved other pods (incl. customer Keycloak). The working
  # single-pod `homechef-temporal` below keeps push notifications running. Re-enable
  # (uncomment) only after the node pool max is raised. Charts retained in git.
  # - homechef-temporal-postgres.yaml
  # - homechef-temporal-ha.yaml
  - homechef-temporal.yaml
  - homechef-auth-bff.yaml
  - homechef-db-seed.yaml
  - homechef-secrets.yaml
  - homechef-web.yaml
  - homechef-web-app.yaml
  - homechef-vendor-portal.yaml
  - homechef-istio.yaml
  - homechef-postgres.yaml
  - homechef-mongodb-crds.yaml
  - homechef-mongodb-operator.yaml
  - homechef-mongodb.yaml
