apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: homechef-api
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: kargo-homechef:prod
  labels:
    app.kubernetes.io/part-of: home-chef-app
    app.kubernetes.io/component: backend
    environment: prod
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: homechef
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    targetRevision: HEAD
    path: charts/apps/homechef-api
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
      parameters:
        - name: image.tag
          value: "main-aed0a60"
        - name: ingress.hosts[0].host
          value: fe3dr.com
        - name: env.HOMECHEF_ENV
          value: "production"
        - name: env.NATS_URL
          value: nats://nats.nats.svc.cluster.local:4222
        - name: env.CORS_ORIGINS
          value: "https://fe3dr.com,https://www.fe3dr.com,https://vendors.fe3dr.com,https://admin.fe3dr.com,https://delivery.fe3dr.com,https://analytics.fe3dr.com"
  destination:
    server: https://kubernetes.default.svc
    namespace: homechef
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
      # RespectIgnoreDifferences deliberately NOT set. argocd-cm ignores
      # .spec.data[]?.remoteRef.* on ExternalSecrets, and with that option on,
      # ArgoCD takes the WHOLE spec.data array from the LIVE object when it
      # applies — so a NEW secret key can never sync. DELIVERY_SURGE_PIN_KEY sat
      # unapplied through 7 self-heals, each logging "configured" successfully.
      # The ignore rules still suppress diff noise; they just no longer strip
      # fields during apply.
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
