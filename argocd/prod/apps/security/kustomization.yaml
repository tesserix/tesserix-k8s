apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Anything listed here must also exist as a file — and any file added here must
# be listed, or Kustomize silently never builds it.
resources:
  - openbao-namespace.yaml
  - openbao.yaml
  - secret-service.yaml
