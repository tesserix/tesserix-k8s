apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# tesserix-social — devtest cluster apps.
# Add a service entry here ONLY when its chart + secrets are ready
# in cluster. Right now: just web. Subsequent PRs will add auth-bff,
# platform-api, reports-agent, postgres.

resources:
  - web.yaml
