apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# HomeChef Namespace - Home Chef/Food Platform
# Food delivery and chef management services

resources:
  # Secrets are now managed by External Secrets Operator
  - homechef-api.yaml
  - homechef-web.yaml
