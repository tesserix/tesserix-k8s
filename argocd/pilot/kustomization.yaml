apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Pilot environment - manages apps and infrastructure via App-of-Apps pattern
resources:
  # Infrastructure App-of-Apps (deployed first)
  - infrastructure-app.yaml
  # Applications App-of-Apps (deployed after infrastructure)
  - apps-app.yaml
