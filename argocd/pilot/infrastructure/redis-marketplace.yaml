apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redis-marketplace
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/part-of: tesserix
    app.kubernetes.io/component: cache
    target-namespace: marketplace
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  project: default
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    path: charts/thirdparty/redis
    targetRevision: HEAD
    helm:
      releaseName: redis
      values: |
        # Redis for Marketplace (Tesseract Hub)
        auth:
          enabled: true
          password: "password"

        resources:
          requests:
            memory: 512Mi
          limits:
            memory: 4Gi

        persistence:
          enabled: true
          storageClass: standard-rwo
          size: 16Gi

        service:
          type: ClusterIP
          port: 6379

  destination:
    server: https://kubernetes.default.svc
    namespace: redis-marketplace
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - Replace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
