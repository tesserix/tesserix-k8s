apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: identity-customer
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/part-of: tesserix
    app.kubernetes.io/component: identity
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://github.com/tesserix/tesserix-k8s.git
    path: charts/thirdparty/identity-customer
    targetRevision: HEAD
    helm:
      releaseName: identity-customer
      valueFiles:
        - values.yaml
      values: |
        # DevTest specific overrides
        keycloak:
          replicaCount: 2
          hostname: "devtest-customer-idp.tesserix.app"

          # Reduce resources for devtest
          resources:
            requests:
              memory: 768Mi
            limits:
              memory: 1536Mi

          # Disable autoscaling for devtest
          autoscaling:
            enabled: false

        # PostgreSQL - smaller for devtest
        keycloak-db:
          primary:
            persistence:
              size: 20Gi
            resources:
              requests:
                memory: 512Mi
              limits:
                memory: 2Gi
          readReplicas:
            replicaCount: 1
            persistence:
              size: 20Gi
          metrics:
            enabled: false  # Disable until image version is fixed

        # Redis - standalone for devtest
        keycloak-redis:
          architecture: standalone  # Use standalone instead of replication
          sentinel:
            enabled: false  # Must disable sentinel for standalone mode
          master:
            persistence:
              size: 2Gi
            resources:
              requests:
                memory: 256Mi
              limits:
                memory: 1Gi
          metrics:
            enabled: false  # Disable until image version is fixed

        # Admin SSO Configuration
        adminSSO:
          enabled: true
          googleCredentialsSecret: "keycloak-google-sso"
          superAdmins:
            - "unidevidp@gmail.com"
            - "mahesh.sangawar@gmail.com"
            - "samyak.rout@gmail.com"
          disablePasswordLogin: false  # Keep password as break-glass

  destination:
    server: https://kubernetes.default.svc
    namespace: identity-customer
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
